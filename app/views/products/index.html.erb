<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Pname</th>
      <th>Pprice</th>
      <th>Psku</th>
      <th>Pimage</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.pname %></td>
        <td><%= product.pprice %></td>
        <td><%= product.psku %></td>
        <td><%= product.pimage %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <tr>
        <%
=begin%>
 here lots of products don't have user id and vlidation is added to model level to bypass this if is used for some products don't have userid this is tmporry 
<%
=end%>
        <small>Created by: <%= product.user.username if product.user %>,
                            <%= time_ago_in_words(product.created_at) %>
                            last updated: <%= time_ago_in_words(product.updated_at) %> ago 
        </small>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<br>

<%= link_to 'New Product', new_product_path %>

